
public class Hipotecario implements Prestamo {

	private int cuota;
	private float sueldo;
	
	public String darPrestamo(float monto,float sueldo, int cuotas) {
		
		cuota = (int)((sueldo * 1.4) / cuotas);
		if(cuotas < (sueldo * 0.4)){
	
			
		
		}


}
